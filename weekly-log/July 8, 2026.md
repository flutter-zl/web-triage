# Web Triage - July 8, 2026

## Weekly Health Checks

- **Unassigned P0/P1 web issues**: 0.
- **Untriaged web PRs, flutter/flutter**: 20. This is above the 15 PR threshold, so the PR queue should be pruned or labeled this week.
- **Untriaged web PRs, flutter/packages**: 1.
- **Stale `waiting for customer response` web issues**: 0.
- **Open P0 weekly update check**: no open unassigned P0s found in the web query.

## Issue Recommendations

### #188264 - flutter run web: full-page redirect back to a nested OIDC callback route hangs
- **Action**: request info
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web
- **Labels to remove**:
- **Title cleanup**: `[web] flutter run hangs on cold-loading nested path routes with DDC modules`
- **Comment**: Keep the current waiting state. The next useful step is the smaller runnable repro and confirmation of whether `flutter run -d web-server` behaves the same as `flutter run -d chrome`.
- **Reasoning**: The report is detailed and likely valid, but the sample is still not complete enough for a maintainer to run.
- **Root cause**: The DDC library-bundle loader appears to resolve per-library module URLs relative to the current nested path during `flutter run`, so the dev server returns `index.html` for module requests and the browser blocks them as scripts. This is probably in the web dev module loading path, with Dart involvement already noted by the `dependency: dart` label.

### #187707 - CupertinoSheet state restoration not working on web
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web
- **Labels to remove**:
- **Title cleanup**:
- **Comment**: Web triage agrees this is already routed outside team-web. Add `triaged-web`; keep ownership with `team-design` unless the design team wants to consolidate it into a broader state-restoration tracking issue.
- **Reasoning**: The issue has `fyi-web` and is already owned and triaged by design, so web triage only needs to acknowledge it.
- **Root cause**: The failing CupertinoSheet test depends on state restoration, which is not currently supported on Flutter web. Existing web skips appear to track the same limitation, but the suggested #33615 reference is not an exact duplicate.

### #188860 - Text overflowed on some broswer
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**: harryterkelsen
- **Labels to add**: triaged-web, P2, has reproducible steps
- **Labels to remove**:
- **Title cleanup**: `[web] WeChat WebView can fail to paint body content when text size is not standard`
- **Comment**:
- **Reasoning**: The issue is now narrowed to WeChat embedded WebView behavior and the reporter found a stable trigger through the WeChat text-size setting.
- **Root cause**: The body content and FAB remain interactive but fail to paint in WeChat WebView when the host app text size is not standard, while stock Safari and Chrome work. This points at a web engine rendering or embedded WebView compositing interaction rather than a Flutter layout overflow.

### #189056 - `FilteringTextInputFormatter.allow` breaks dead key composition on Flutter Web
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web, P2, found in release: 3.38
- **Labels to remove**:
- **Title cleanup**:
- **Comment**:
- **Reasoning**: The issue is already routed to `team-text-input` with `fyi-web`, and it has clear repro steps, crash output, and a web-specific dead-key composition path.
- **Root cause**: During dead-key composition on web, the formatter rejects or shortens the text while the composing range still points past the new text length. The framework then receives an invalid editing state and trips the composing-range assertion.

### #189064 - Support launching Flutter Web in an existing browser window/tab while preserving Hot Reload
- **Action**: re-route
- **Priority**: P3
- **Suggested assignee**:
- **Labels to add**: triaged-web, team-tool, P3
- **Labels to remove**: team-web
- **Title cleanup**:
- **Comment**: This is a web developer experience request, but the implementation and ownership are in the Flutter tool browser-launch/debugging workflow. Keep `platform-web` and re-route to `team-tool`.
- **Reasoning**: The request is an opt-in feature proposal for `flutter run -d chrome`, not a web engine or framework bug.
- **Root cause**:

### #188874 - Wrong TextRange result in TextPainter on web
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**: harryterkelsen
- **Labels to add**: triaged-web, P2, engine, c: rendering, has reproducible steps, found in release: 3.44
- **Labels to remove**:
- **Title cleanup**: `[web] TextPainter.getLineBoundary returns previous line at a line break offset`
- **Comment**:
- **Reasoning**: The issue has a small TextPainter repro and shows a web-only line-boundary mismatch against macOS.
- **Root cause**: The web paragraph line-boundary path appears to treat an offset at the end of the first visual line as belonging to that first line, while native returns the next line boundary. A Flame workaround exists, but the underlying Flutter web text metric behavior remains wrong.

## Triage Summary - July 8, 2026

- Triaged: 4 issues
- Close: 0 issues
- Request info: 1 issue
- Re-route: 1 issue
- Remaining untriaged: 0 after applying the recommended labels

## PR Recommendations

### flutter/flutter

- **#182861 - [web] Roll Chrome to 145 (framework)**: Add `triaged-web`. It is old, blocked, and has changes requested from harryterkelsen, so no triage action beyond labeling.
- **#183835 - Use dart2wasm app.support.js expression with `js-string` builtin requirement**: Add `triaged-web` and likely `team-web`. It is approved but stale since mid-June, so ask the author if it is still intended to land.
- **#184961 - Fix rendering custom element size**: Add `triaged-web` and `team-web`. Review is requested from mdebbar and harryterkelsen; flag for design review because the author explicitly is unsure whether this is the right layer.
- **#185080 - Fix: activateSystemCursor should set cursor on all views**: Add `triaged-web`. It is approved, so next action is merge or unblock whatever is preventing autosubmit.
- **#185622 - [web] Fix multi-view sizing race condition (Lock approach)**: Add `triaged-web`. Scope is clear and web-owned, but the review points at a race in the chosen lock boundary, so it needs author follow-up.
- **#186108 - [skwasm][canvaskit] Honor Paint.filterQuality in Canvas.drawAtlas**: Add `triaged-web`. It is approved and web-owned; no triage blocker.
- **#186540 - Add visible focus ring for keyboard-focused semantics nodes**: Add `triaged-web`. Keep accessibility and web reviewers engaged; the current design concern is whether the focus indicator belongs in framework rather than semantics DOM.
- **#187108 - [web] Autofill all AutofillGroup fields in semantics mode**: Add `triaged-web`. It is approved by accessibility and still has a yjbanov review request, so verify whether that request is still needed.
- **#187181 - Drawing decorations directly on the target Canvas**: Add `triaged-web`. Request title cleanup, for example `[web] Draw paragraph decorations on the target Canvas`, and ask for reviewer follow-up.
- **#188010 - refactor(web): use singular crossOriginStorage.requestFileHandle()**: Add `triaged-web`. It is approved but blocked; next action is resolving the merge or CI blocker.
- **#188032 - Fixed couple of bugs**: Add `triaged-web`. Request title cleanup before review continues, for example `[web] Fix RTL GetBoxesForRange and GetPositionForGlyph`.
- **#188337 - [web] Avoid absolute positioning for base CanvasKit canvas**: Add `triaged-web`. It has approval after earlier changes requested; next action is merge-readiness or CI follow-up.
- **#188544 - [Impeller] Add filterQuality support to ImageFilter.shader**: Re-route away from web unless there is a web-specific change not shown in the title or body. Consider removing `platform-web` and `team-web`; keep engine rendering ownership with the existing b-luk review path.
- **#188573 - [web] Unify image decoding and codecs on CanvasKit and Skwasm**: Add `triaged-web`. Scope is clear and web-owned; review is requested from flutter-zl.
- **#188738 - [web] Preserve text field focus across tab switches**: Add `triaged-web`. It has recent author activity and needs normal review follow-up.
- **#188761 - [web] Drive Flutter text field from semantics input events for automation**: Add `triaged-web`. It is waiting for review direction from framework/text-input owners and already has `waiting for code freeze`.
- **#188797 - [web] Repair RenderCanvas CSS size drift**: Add `triaged-web`. It is approved and clean; no triage blocker.
- **#188890 - [web] Fall back to full CJK fonts for characters not covered by split slices**: Add `triaged-web`. Review is requested from flutter-zl; no stale blocker.
- **#188910 - Adds missing await on `instantiateImageCodecFromBuffer`**: Add `triaged-web`. The title and body are generic, and there is no human review yet; request a web engine reviewer and clarify the user-visible risk.
- **#189086 - refactor(web): SkwasmPath and SkwasmPathBuilder separation**: Add `triaged-web`. Newly opened with mdebbar requested; no stale blocker.

### flutter/packages

- **#7950 - [camera_web] Re: Support for camera stream on web**: Keep routed to mdebbar, but flag as long-lived. Ask the author to resolve conflicts and refresh the branch before review continues.
