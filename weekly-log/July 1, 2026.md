# Web Triage - July 1, 2026

## Weekly Health Checks

- **Unassigned P0/P1 web issues**: 0.
- **Untriaged web PRs, flutter/flutter**: 20. This is above the 15 PR threshold, so the PR queue should be pruned or labeled this week.
- **Untriaged web PRs, flutter/packages**: 1.
- **Stale `waiting for customer response` web issues**: 0.
- **Open P0 weekly update check**: no open unassigned P0s found in the web query.

## Issue Recommendations

### #188264 - flutter run web: full-page redirect back to a nested OIDC callback route hangs
- **Action**: request info
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web
- **Labels to remove**:
- **Title cleanup**: `[web] flutter run hangs on cold-loading nested path routes with DDC library-bundle modules`
- **Comment**: The current follow-up asking for a smaller repro and `web-server` comparison is the right next step. Once the reporter confirms whether `flutter run -d web-server` also fails and provides a runnable sample, this can move from waiting state to active investigation.
- **Reasoning**: The report is detailed and likely valid, but the current sample is not complete enough for a maintainer to run.
- **Root cause**: The DDC library-bundle loader appears to resolve per-library module URLs relative to the current nested path during `flutter run`, so the dev server returns `index.html` for module requests and the browser blocks them as scripts. This is probably in the web dev module loading path, with Dart involvement already noted by the `dependency: dart` label.

### #187707 - CupertinoSheet state restoration not working on web
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web
- **Labels to remove**:
- **Title cleanup**:
- **Comment**: Web triage agrees this is already routed outside team-web. Add `triaged-web`; keep ownership with `team-design` unless the design team wants to consolidate it into a broader state-restoration tracking issue.
- **Reasoning**: The issue has `fyi-web` and is already owned and triaged by design, so web triage only needs to acknowledge it.
- **Root cause**: The failing CupertinoSheet test depends on state restoration, which is not currently supported on Flutter web. Existing web skips appear to track the same limitation.

### #188666 - [material_ui] `text_field_test` failures on web
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web, P2, a: text input
- **Labels to remove**:
- **Title cleanup**:
- **Comment**:
- **Reasoning**: This is already routed to `team-text-input` with `fyi-web`, and web triage should close out the web review.
- **Root cause**: The skipped Material UI tests exercise web text selection toolbar behavior that is already disabled in flutter/flutter web test runs. This looks like a text-selection behavior gap on web rather than a general web engine routing issue.

### #188682 - Provide solution when this issue occurs DartError: LateInitializationError: Field '_handledContextLostEvent' has not been initialized.
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**: harryterkelsen
- **Labels to add**: triaged-web, P2, engine, e: web_canvaskit, c: crash
- **Labels to remove**:
- **Title cleanup**: `[web][CanvasKit] Widget Inspector hot restart can crash on context-lost handling`
- **Comment**:
- **Reasoning**: The issue now has confirmed repro steps involving Widget Inspector selection plus repeated hot restart, and the stack points at CanvasKit context-lost handling.
- **Root cause**: The CanvasKit surface context-lost callback reads `_handledContextLostEvent` after a hot-restart or inspector-driven lifecycle path where the field was not initialized or the surface lifecycle was disrupted. The failure occurs in the web engine CanvasKit surface/context-loss path, not in the sample app's dialog code.

### #188744 - [Web][CanvasKit] Path API intermittently "is not a function" on ANGLE-Metal
- **Action**: request info
- **Priority**: P2
- **Suggested assignee**: harryterkelsen
- **Labels to add**: triaged-web, P2
- **Labels to remove**:
- **Title cleanup**:
- **Comment**: This is useful production evidence, but it still needs a maintainer-reproducible URL or a smaller sample. Ask the reporter to share the production URL and exact route/device steps privately with a maintainer if it cannot be posted publicly.
- **Reasoning**: The stack traces are credible and renderer-specific, but the issue is still marked `needs repro info` and does not include a local repro.
- **Root cause**: CanvasKit path and picture APIs become unavailable at runtime on some ANGLE-Metal clients, causing blank or partially blank rendering even though Flutter started. This is likely a CanvasKit initialization or GPU backend state issue, and it is related to but not a duplicate of the ANGLE-Vulkan Samsung issue #188164 because the affected GPU/browser family differs.

### #188723 - Mobile Safari Skwasm canvas CSS size collapses to ~0.328px while backing store is valid
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**: harryterkelsen
- **Labels to add**: triaged-web, P2, e: web_skwasm, browser: safari
- **Labels to remove**:
- **Title cleanup**:
- **Comment**:
- **Reasoning**: The issue is actionable, has a clear root-cause hypothesis, and already has a focused draft fix.
- **Root cause**: `RenderCanvas` can keep a stale inline CSS size when the backing store and DPR have not changed, leaving a valid Skwasm canvas rendered at a near-zero visible size on Mobile Safari.
- **PR review**: PR #188797 addresses the likely root cause by checking and repairing logical CSS size even on the same backing-store size path. The test covers the reported 1206 x 2142 at DPR 3 collapse and the change is small, but the PR is still draft and should be reviewed carefully for whether comparing serialized CSS strings is robust enough across browsers.

### #188789 - Expose Flutter Web `<canvas>` DOM elements
- **Action**: triage
- **Priority**: P3
- **Suggested assignee**:
- **Labels to add**: triaged-web, P3, c: new feature
- **Labels to remove**:
- **Title cleanup**:
- **Comment**:
- **Reasoning**: This is a clear feature proposal with a concrete use case, but it is new API surface rather than a bug.
- **Root cause**:

### #188781 - Input box issue in Flutter Web under iOS 27 system
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**:
- **Labels to add**: triaged-web, P2, has reproducible steps, found in release: 3.44
- **Labels to remove**:
- **Title cleanup**: `[web][iOS] TextField cursor gets stuck after long-press drag and keyboard dismissal`
- **Comment**:
- **Reasoning**: The issue has a minimal TextField sample, device/version details, video, and independent verification, and it is already routed to `team-text-input`.
- **Root cause**: The iOS web text-editing path appears to lose cursor or selection state after a long-press drag and keyboard dismissal. The field remains partially usable until the keyboard closes, then subsequent focus attempts stop moving the caret or reopening the keyboard.

### #188859 - `ensureSemantics()` breaks `Tap*Details` emitted from `GestureDetector` on mouse click on web
- **Action**: triage
- **Priority**: P2
- **Suggested assignee**: flutter-zl
- **Labels to add**: triaged-web, P2, engine
- **Labels to remove**:
- **Title cleanup**:
- **Comment**: This is related to #146063 and duplicate #177314, but it is not an exact duplicate because this report is about incorrect `TapDownDetails` position and pointer kind on a plain `GestureDetector`, not merged semantics routing inside `RadioListTile`.
- **Reasoning**: The sample is minimal, web-specific, and points to the semantics pointer path affecting app gesture data.
- **Root cause**: When semantics is forced on, the web engine appears to synthesize or reroute pointer events through the semantics activation path, producing center-based coordinates and `PointerDeviceKind.unknown` instead of the actual mouse click details.

## Triage Summary - July 1, 2026

- Triaged: 7 issues
- Close: 0 issues
- Request info: 2 issues
- Re-route: 0 issues
- Remaining untriaged: 0 after applying the recommended labels

## PR Recommendations

### flutter/flutter

- **#182861 - [web] Roll Chrome to 145 (framework)**: Add `triaged-web`. It is old and blocked with no body; ask the author to fill in scope and current blocker before review continues.
- **#183835 - Use dart2wasm app.support.js expression with `js-string` builtin requirement**: Add `triaged-web` and likely `team-web`. Approved, so no web triage blocker.
- **#184961 - Fix rendering custom element size**: Add `triaged-web` and `team-web`. Review is requested from mdebbar; the PR description admits uncertainty about layer, so flag for design review before code review.
- **#185080 - Fix: activateSystemCursor should set cursor on all views**: Add `triaged-web`. Approved but unstable; next action is CI follow-up.
- **#185622 - [web] Fix multi-view sizing race condition (Lock approach)**: Add `triaged-web`. Blocked and review required; owner is clear.
- **#186108 - [skwasm][canvaskit] Honor Paint.filterQuality in Canvas.drawAtlas**: Add `triaged-web`. Approved; no triage blocker.
- **#186252 - engine: warn on WASM load failure when not crossOriginIsolated**: Add `triaged-web`. Approved; no triage blocker.
- **#186254 - engine: explain why each candidate build was skipped in Flutter web loader**: Add `triaged-web`. Approved; no triage blocker.
- **#186540 - Add visible focus ring for keyboard-focused semantics nodes**: Add `triaged-web` and `team-web`. Review is requested from accessibility/framework owners and mdebbar; no staleness blocker.
- **#187108 - [web] Autofill all AutofillGroup fields in semantics mode**: Add `triaged-web`. Approved with a review request still present; verify whether the stale request can be cleared.
- **#187181 - Drawing decorations directly on the target Canvas**: Add `triaged-web`. Needs a clearer title and reviewer follow-up; assignee is the author.
- **#187459 - [web] Fix grouped autofill on iOS Chrome**: Add `triaged-web`. Review requested from mdebbar; no triage blocker.
- **#188010 - refactor(web): use singular crossOriginStorage.requestFileHandle()**: Add `triaged-web`. Approved but blocked; next action is resolving whatever blocks merge.
- **#188032 - Fixed couple of bugs**: Add `triaged-web`. Request title cleanup before review, for example `[web] Fix RTL GetBoxesForRange and GetPositionForGlyph`.
- **#188337 - [web] Avoid absolute positioning for base CanvasKit canvas**: Add `triaged-web`. Changes requested and blocked; next action is author response.
- **#188544 - [Impeller] Add filterQuality support to ImageFilter.shader**: Add `triaged-web` only if web triage wants to keep tracking it because it has `platform-web`; otherwise consider removing `platform-web` if the change is not web-specific. Changes requested.
- **#188573 - [web] Unify image decoding and codecs on CanvasKit and Skwasm**: Add `triaged-web`. Scope is clear and web-owned; needs normal review assignment.
- **#188738 - [web] Preserve text field focus across tab switches**: Add `triaged-web`. Blocked and review required; no triage blocker.
- **#188803 - Fixing multiline GetBoxesForRange**: Add `triaged-web`. Approved but blocked; title should be cleaned up before merge if possible.
- **#188868 - Fixing GetLineBoundaries with hard line break at the end**: Add `triaged-web`. Newly opened, blocked, and missing body; request a short description with linked issue and test plan.

### flutter/packages

- **#7950 - [camera_web] Re: Support for camera stream on web**: Keep routed to mdebbar, but flag as long-lived and dirty. Ask the author to resolve conflicts and refresh the branch before review continues.
